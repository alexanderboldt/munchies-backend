info:
  name: Update
  type: http
  seq: 4

http:
  method: PUT
  url: "{{BASE_URL}}/recipes/:id"
  headers:
    - name: X-API-VERSION
      value: "1"
  params:
    - name: id
      value: "1"
      type: path
  body:
    type: json
    data: |-
      {
        "labelId": null,
        "title": "Beanburger",
        "description": "Muss lecker und GESUND sein!",
        "duration": 3600
      }
  auth:
    type: bearer
    token: "{{ACCESS_TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
