info:
  name: Read All
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_URL}}/recipes/:recipe_id/steps"
  headers:
    - name: X-API-VERSION
      value: "1"
  params:
    - name: recipe_id
      value: "2"
      type: path
  auth:
    type: bearer
    token: "{{ACCESS_TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
