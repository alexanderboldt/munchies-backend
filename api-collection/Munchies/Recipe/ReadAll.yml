info:
  name: Read All
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_URL}}/recipes"
  headers:
    - name: X-API-VERSION
      value: "1"
  auth:
    type: bearer
    token: "{{ACCESS_TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
