info:
  name: Login
  type: http
  seq: 1

http:
  method: POST
  url: "{{BASE_URL}}/auth/login"
  body:
    type: form-urlencoded
    data:
      - name: grant_type
        value: password
      - name: client_id
        value: munchies-client
      - name: client_secret
        value: BZPHEYae2dJHdgqZCe671oamDqWzqk73
      - name: username
        value: barry
      - name: password
        value: barry

runtime:
  scripts:
    - type: after-response
      code: bru.setEnvVar("ACCESS_TOKEN", res.body.access_token);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
