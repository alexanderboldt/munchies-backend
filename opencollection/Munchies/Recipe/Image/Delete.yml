info:
  name: Delete
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{BASE_URL}}/recipes/:id/images"
  headers:
    - name: X-API-VERSION
      value: "1"
  params:
    - name: id
      value: "2"
      type: path
  auth:
    type: bearer
    token: "{{ACCESS_TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
