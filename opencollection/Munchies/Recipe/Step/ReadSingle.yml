info:
  name: Read Single
  type: http
  seq: 3

http:
  method: GET
  url: "{{BASE_URL}}/recipes/:recipe_id/steps/:step_id"
  headers:
    - name: X-API-VERSION
      value: "1"
  params:
    - name: recipe_id
      value: "2"
      type: path
    - name: step_id
      value: "3"
      type: path
  auth:
    type: bearer
    token: "{{ACCESS_TOKEN}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
